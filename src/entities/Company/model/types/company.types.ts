export type CompanyCardData = {
  id: string
  name: string
  rating: string
  reviews: string
  city: string
  description: string
}

export type CatalogFilterItem = {
  id: string
  label: string
  isSelected?: boolean
}
